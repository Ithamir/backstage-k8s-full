image:
  repository: my-registry/backstage
  tag: "2.5.0"
  pullSecrets:
    - my-registry-creds
    - other-secret
github:
  auth:
    create: true
    token: test-token
postgres:
  auth:
    password: test-pass
