apiVersion: apps/v1
kind: Deployment
{{- $githubSecretName := include "backstage.githubSecretName" . }}
{{- $githubOAuthSecretName := include "backstage.githubOAuthSecretName" . }}
{{- $rbacAdminUser := .Values.rbac.adminUser }}
metadata:
  name: {{ include "backstage.fullname" . }}
  labels:
    {{- include "backstage.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "backstage.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/rbac-config: {{ include (print $.Template.BasePath "/rbac-configmap.yaml") . | sha256sum }}
      labels:
        {{- include "backstage.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "backstage.fullname" . }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: backstage
          image: "{{ include "backstage.image" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - "node"
          args:
            - "packages/backend"
            - "--config"
            - "app-config.yaml"
            - "--config"
            - "/etc/backstage/app-config.runtime.yaml"
            {{- if $rbacAdminUser }}
            - "--config"
            - "/etc/backstage/app-config.admin.yaml"
            {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
          ports:
            - name: http
              containerPort: 7007
          envFrom:
            - configMapRef:
                name: platform-identity
            - secretRef:
                name: {{ include "backstage.postgresSecretName" . }}
            - secretRef:
                name: {{ $githubSecretName }}
            {{- if ne $githubSecretName $githubOAuthSecretName }}
            - secretRef:
                name: {{ $githubOAuthSecretName }}
            {{- end }}
          volumeMounts:
            - name: app-config
              mountPath: /etc/backstage
              readOnly: true
            - name: rbac-config
              mountPath: /etc/backstage/rbac
              readOnly: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- if .Values.probes.enabled }}
          readinessProbe:
            httpGet:
              port: {{ .Values.probes.readiness.port }}
              path: {{ .Values.probes.readiness.path }}
          livenessProbe:
            httpGet:
              port: {{ .Values.probes.liveness.port }}
              path: {{ .Values.probes.liveness.path }}
          {{- end }}
      volumes:
        - name: app-config
          configMap:
            name: {{ include "backstage.fullname" . }}-app-config
        - name: rbac-config
          configMap:
            name: {{ include "backstage.fullname" . }}-rbac
