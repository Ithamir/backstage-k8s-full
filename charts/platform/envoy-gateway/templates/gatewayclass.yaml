{{- with .Values.gatewayClass }}
{{- if .enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ .name }}
spec:
  controllerName: {{ .controllerName }}
  parametersRef:
    {{- toYaml .parametersRef | nindent 4 }}
{{- end }}
{{- end }}
