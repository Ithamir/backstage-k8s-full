auth:
  providers:
    github:
      development:
        clientId: github-oauth-client-id
        clientSecret: github-oauth-client-secret

permission:
  rbac:
    admin:
      users:
        - user:default/itamar-ratson
    policies-csv-file: ./rbac-policies.csv

catalog:
  locations:
    - type: file
      target: ../../catalog-info.yaml
    - type: file
      target: ../catalog-info.yaml
    - type: file
      target: ../../charts/edge-gateway/catalog-info.yaml
    - type: file
      target: ../../users.yaml
